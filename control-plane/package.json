{
  "name": "openclaw-delegation-control-plane",
  "version": "0.2.0-mvp",
  "private": true,
  "type": "module",
  "description": "A lightweight control plane for OpenClaw main/worker routing modes, policy enforcement and monitoring.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test",
    "hash-password": "node src/cli.mjs hash-password",
    "doctor": "node src/cli.mjs doctor"
  }
}
